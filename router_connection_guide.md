# 持ち込みルーターでの接続手順

文化祭の教室でスマホからアクセスする場合、学校の Wi‑Fi だとローカルサーバーの通信が制限されることがあります。 そのため、持ち込みルーターを使って同じネットワーク内にするのが最も確実です。

## 1. ルーターをセットアップする

1. 持ち込みルーターの電源を入れる
2. Wi‑Fi が起動するまで待つ
3. ルーター名（SSID）とパスワードを確認する

よくある例:
- SSID: "HomeRouter_1234"
- パスワード: "abc12345"

## 2. PC とスマホを同じ Wi‑Fi に接続する

- PC をルーターの Wi‑Fi に接続
- スマホも同じ Wi‑Fi に接続

これで、PC とスマホは同じ LAN 内に入ります。

## 3. PC の IP アドレスを確認する

Windows の場合、ターミナルで次を実行します。

```bash
ipconfig
```

表示された Wi‑Fi の欄から IPv4 アドレスを確認します。

例:

```text
IPv4 アドレス . . . . . . . . . . : 192.168.11.20
```

この値が PC のローカルアドレスです。

## 4. ローカルサーバーを起動する

このプロジェクトのフォルダで、次を実行します。

```bash
python -m http.server 8000
```

または、[start_local_server.bat](start_local_server.bat) をダブルクリックして起動します。

## 5. スマホのブラウザでアクセスする

スマホで次のURLを開きます。

```text
http://192.168.11.20:8000
```

もし PC の IP が `192.168.11.20` なら、上の URL でアクセスできます。

## 6. 使うときの注意

- 学校の Wi‑Fi ではなく、持ち込みルーターの Wi‑Fi を使う
- 同じネットワーク内の端末だけが見える
- ルーターの Wi‑Fi を共有した状態であれば、複数人でも使いやすい

## 7. もっと簡単にするには

- ルーターの Wi‑Fi を名前が分かりやすいものにする
- 事前に PC の IP をメモしておく
- スマホでアクセスするURLを張り紙しておく

## 8. まとめ

最短の対処法は次の通りです。

1. 持ち込みルーターを起動
2. PC とスマホを同じ Wi‑Fi に接続
3. PC の IP を確認
4. `python -m http.server 8000` を実行
5. スマホで `http://<PCのIP>:8000` を開く

これが、学校用 Wi‑Fi 制限を回避して、スムーズに使う最も現実的な方法です。
